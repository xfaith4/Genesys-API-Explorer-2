# Legacy Insight Packs Folder

Insight packs moved to `insights/packs/`.

If you have older scripts referencing `.\insightpacks\*.json`, keep using the same filename — `Invoke-GCInsightPack` will try to resolve legacy paths automatically.

